<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="styles/styles.css">
    <title>New App</title>
</head>

<body>
    <h1>Click For a New Joke !</h1>
    <div class="container">
        <div class="box">
            <%if(data.joke){  %>
            <p><%= data.joke  %></p>
            <%  }else{ %>
            <%if (data.flags.nsfw === true){  %>
            <p><%= data.setup  %></p>
            <p><%= data.delivery  %></p>
            <span> NSFW</span>
            <% }else{  %>
            <p><%= data.setup  %></p>
            <p id="delivery"><%= data.delivery  %></p>
            <% } %>
            <%}  %>

        </div>
        <form action="/" method="get">
            <input type="submit" value="Get Joke" method="post">
        </form>

    </div>
</body>

</html>